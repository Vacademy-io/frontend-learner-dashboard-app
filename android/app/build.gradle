apply plugin: 'com.android.application'

android {
    namespace "io.vacademy.student.app"
    compileSdk rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "io.vacademy.student.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 16
        versionName "2.0.1"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        aaptOptions {
             // Files and dirs to omit from the packaged assets dir, modified to accommodate modern web apps.
             // Default: https://android.googlesource.com/platform/frameworks/base/+/282e181b58cf72b6ca770dc7ca5f91f135444502/tools/aapt/AaptAssets.cpp#61
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }

		// Default manifest placeholders (can be overridden per flavor)
		manifestPlaceholders = [
			DEEP_LINK_HOST: "learner.vacademy.io"
		]
    }
	// Signing configuration for Seven CS
	signingConfigs {
		seven_cs_release {
			storeFile file("../../seven_cs/seven_cs_release.keystore")
			storePassword System.getenv("SEVEN_CS_STORE_PASSWORD") ?: "ask_team_for_password"
			keyAlias System.getenv("SEVEN_CS_KEY_ALIAS") ?: "ask_team_for_alias"
			keyPassword System.getenv("SEVEN_CS_KEY_PASSWORD") ?: "ask_team_for_password"
		}
	}

    buildTypes {
        debug {
            minifyEnabled false
            debuggable true
            // Debug builds should not use release signing
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            debuggable false
            // Apply signing configuration for release builds
            signingConfig signingConfigs.seven_cs_release
        }
    }

	// White-label flavors
	flavorDimensions "client"
	productFlavors {
		ssdc {
			dimension "client"
			applicationId "io.vacademy.student.app"
			// Deep link host for this client
			manifestPlaceholders.DEEP_LINK_HOST = "learner.vacademy.io"
		}

		seven_cs {
			dimension "client"
			applicationId "com.sevencs.learner"
			resValue "string", "app_name", "The 7 Cs"
			manifestPlaceholders.DEEP_LINK_HOST = "7cs.learner.io"
			
			// Apply signing configuration for release builds
			signingConfig signingConfigs.seven_cs_release
		}
	}
}

repositories {
    flatDir{
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')

    // Firebase Cloud Messaging for push notifications
    implementation platform('com.google.firebase:firebase-bom:33.2.0')
    implementation 'com.google.firebase:firebase-messaging'
}

apply from: 'capacitor.build.gradle'
apply plugin: 'com.google.gms.google-services'
